# build artifacts and caches (the generated/ dir is checked in: the
# submission manifest references its pinned offline sources)
.flutter-sdk/
flet-src/
client-build/
build-dir/
repo/
.flatpak-builder/
*.flatpak
*.log
