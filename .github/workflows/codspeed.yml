name: CodSpeed

on:
  push:
    branches:
      - "main"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  benchmarks:
    name: Run benchmarks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python 3.14.3 (free-threaded)
        uses: actions/setup-python@v7
        with:
          python-version: "3.14.3t"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-codspeed
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run benchmarks
        uses: CodSpeedHQ/action@v4.18.5
        with:
          mode: simulation
          run: pytest benchmarks/ --codspeed
