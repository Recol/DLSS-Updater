name: Star History

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: star-history
  cancel-in-progress: false

jobs:
  star-history:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      # Renders the star-history chart in CI with the repo's own token and
      # commits a static SVG, since GitHub's 2026-06-30 stargazers API
      # restriction broke star-history.com's hosted chart endpoints.
      - uses: narayann7/star-history-action@deb4d27dc8ef0cd6415d44b3ede9aa7f2c8ecaba # v1.0.2
        with:
          repos: ${{ github.repository }}
